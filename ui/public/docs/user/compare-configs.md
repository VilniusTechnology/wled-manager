# Compare Configs

**Compare Configs** lets you compare the *current* configuration of a device against a stored backup or another device's configuration.

## Use Cases

- **Drift Detection**: See if a device's settings have been changed since the last backup.
- **Device Cloning**: Compare a template device with a new device to ensure settings match.

You can also use this to compare one device's settings with another.